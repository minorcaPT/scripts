funky = 1
repeat 
funky = funky + 1
game:GetService("ReplicatedStorage").Remotes.DumpClicks:InvokeServer(80)
wait(1)
until funky > 100 --change the 100 to the ammount of seconds u wanna run it for (it will click 80 a second) (its the max the game registers)--
